typo.ir {
    encode gzip zstd

    request_body {
        max_size 75MB
    }

    reverse_proxy typo-real:8000

    header {
        X-Content-Type-Options nosniff
        X-Frame-Options SAMEORIGIN
        Referrer-Policy strict-origin-when-cross-origin
        Permissions-Policy "camera=(), microphone=(), geolocation=()"
    }
}
